
var ds = require('process-dataset');

module.exports = function () {
    ds.set('fivefive', '5555');
};
module.exports.dump = function () {
    ds.dump();
};
module.exports.fixdataset = function () {
    // dataset sealed. not works
    ds.abc = '123';
    ds.qwe = '456';
    ds.method = function (a, b) {
        console.log({a: a, b: b});
    }
    // works
    ds.set('projects', ['lib/a', 'lib/b', 'c']);
};
